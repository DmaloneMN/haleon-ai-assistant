*** Begin Patch
*** Add File: src/haleon_assistant/agents/__init__.py
+"""
+Agents package for haleon_assistant.
+Contains lightweight, deterministic stubs for initial development and testing.
+"""
+
*** End Patch
*** Begin Patch
*** Add File: src/haleon_assistant/agents/orchestrator.py
+from typing import List, Dict
+
+from haleon_assistant.agents.triage_agent import TriageAgent
+from haleon_assistant.agents.retrieval_agent import RetrievalAgent
+from haleon_assistant.agents.safety_agent import SafetyAgent
+from haleon_assistant.agents.synthesis_agent import SynthesisAgent
+
+
+class Orchestrator:
+    """Simple orchestrator that wires together triage, retrieval, safety, and synthesis."""
+
+    def __init__(self) -> None:
+        self.triage = TriageAgent()
+        self.retrieval = RetrievalAgent()
+        self.safety = SafetyAgent()
+        self.synthesis = SynthesisAgent()
+
+    def run(self, query: str) -> Dict:
+        """Run the basic pipeline and return a result dict with answer and citations."""
+        _ = query or ""
+        route = self.triage.classify(query)
+        docs = self.retrieval.retrieve(query)
+        safety = self.safety.check(query, docs)
+        if not safety.get("ok", True):
+            return {"answer": "Content blocked by safety policy", "citations": []}
+        answer = self.synthesis.synthesize(query, docs)
+        return {"answer": answer, "citations": docs, "route": route}
+
*** End Patch
*** Begin Patch
*** Add File: src/haleon_assistant/agents/triage_agent.py
+class TriageAgent:
+    """Minimal intent classifier for scaffold/testing purposes."""
+
+    def classify(self, query: str) -> dict:
+        if not query:
+            return {"intent": "general"}
+        lower = query.lower()
+        if "dose" in lower or "dosage" in lower:
+            return {"intent": "dosage"}
+        if "adverse" in lower or "reaction" in lower:
+            return {"intent": "pharmacovigilance"}
+        return {"intent": "general"}
+
*** End Patch
*** Begin Patch
*** Add File: src/haleon_assistant/agents/retrieval_agent.py
+class RetrievalAgent:
+    """Placeholder retrieval agent that returns deterministic example documents."""
+
+    def retrieve(self, query: str) -> list:
+        # Return a reproducible sample "document" for testing
+        return [
+            {
+                "title": "Sample Doc",
+                "url": "https://example.org/doc",
+                "snippet": "Example snippet relevant to query: " + (query or ""),
+            }
+        ]
+
*** End Patch
*** Begin Patch
*** Add File: src/haleon_assistant/agents/safety_agent.py
+class SafetyAgent:
+    """Simple scaffold safety agent; always returns ok=True in this scaffold."""
+
+    def check(self, query: str, docs: list) -> dict:
+        # Real checks (PII, toxicity, medical risk) should be implemented later.
+        return {"ok": True, "issues": []}
+
*** End Patch
*** Begin Patch
*** Add File: src/haleon_assistant/agents/synthesis_agent.py
+from typing import List
+
+
+class SynthesisAgent:
+    """Compose a short answer from retrieved document snippets."""
+
+    def synthesize(self, query: str, docs: List[dict]) -> str:
+        snippets = [d.get("snippet", "") for d in docs if isinstance(d, dict)]
+        joined = " ".join(s for s in snippets if s)
+        base = f"Placeholder answer for: {query}" if query else "Placeholder answer"
+        if joined:
+            return f"{base}. Sources: {joined}"
+        return base
+
*** End Patch
*** Begin Patch
*** Add File: src/haleon_assistant/agents/pharmacovigilance_agent.py
+class PharmacovigilanceAgent:
+    """Stub pharmacovigilance scanner."""
+
+    def scan(self, text: str) -> dict:
+        # No adverse events detected in scaffold
+        return {"ae_detected": False, "details": []}
+
*** End Patch
*** Begin Patch
*** Add File: tests/unit/test_triage_agent.py
+from haleon_assistant.agents.triage_agent import TriageAgent
+
+
+def test_triage_detects_dosage_intent():
+    t = TriageAgent()
+    out = t.classify("What is the dosage?")
+    assert out.get("intent") == "dosage"
+
*** End Patch
*** Begin Patch
*** Add File: tests/unit/test_retrieval_agent.py
+from haleon_assistant.agents.retrieval_agent import RetrievalAgent
+
+
+def test_retrieval_returns_documents():
+    r = RetrievalAgent()
+    docs = r.retrieve("aspirin dose")
+    assert isinstance(docs, list) and len(docs) > 0
+    assert "snippet" in docs[0]
+
*** End Patch
*** Begin Patch
*** Add File: tests/unit/test_safety_agent.py
+from haleon_assistant.agents.safety_agent import SafetyAgent
+
+
+def test_safety_passes_on_benign_input():
+    s = SafetyAgent()
+    result = s.check("hello", [])
+    assert result.get("ok") is True
+
*** End Patch
